export declare class AppService {
    private _cache;
    private getUsers;
    getHello(): string;
    fetchAppLoad(name: string): AppLoadResponseType;
    updateBalance(name: string, amount: number): string;
}
